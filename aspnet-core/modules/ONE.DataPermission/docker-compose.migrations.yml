version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=DataPermission_Identity
      - DataPermission_DB=DataPermission_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
